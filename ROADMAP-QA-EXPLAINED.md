# 🗺️ คู่มืออ่าน roadmap.sh/QA ฉบับสมบูรณ์

> อธิบายทุกหัวข้อบนแผนที่ QA Engineer จาก roadmap.sh

---

## 📍 roadmap.sh คืออะไร?

**roadmap.sh** = เว็บไซต์แสดง **"แผนที่การเรียนรู้"** (Learning Roadmap) สำหรับอาชีพต่างๆ ในโลกเทคโนโลยี

**หน้าตาเป็นแบบนี้:**
```
🗺️ [START] ──▶ [Topic 1] ──▶ [Topic 2] ──▶ ... ──▶ [ADVANCED]
                │              │
                ▼              ▼
           [Sub-topic]    [Sub-topic]
```

**สีบนแผนที่หมายถึง:**
- 🟢 **สีเขียว** = ควรรู้ (Must know)
- 🟡 **สีเหลือง** = ควรรู้เพิ่ม (Good to know)
- 🔵 **สีฟ้า** = ทางเลือก (Optional)
- ⚪ **สีขาว/เทา** = ยังไม่ต้องรู้ก็ได้

---

## 🎯 QA Roadmap มีหัวข้ออะไรบ้าง?

### **ส่วนที่ 1: พื้นฐาน (Fundamentals)** 🟢

#### 1. Internet Basics (พื้นฐานอินเทอร์เน็ต)
**คืออะไร:** ต้องเข้าใจก่อนว่าเว็บทำงานยังไง

| หัวข้อย่อย | คืออะไร | ทำไมต้องรู้ |
|-----------|---------|-------------|
| **HTTP/HTTPS** | โปรโตคอลส่งข้อมูลบนเว็บ | รู้ว่า request/response ทำงานยังไง |
| **DNS** | แปลงชื่อโดเมนเป็น IP | เข้าใจว่าทำไมพิมพ์ google.com ถึงเข้าเว็บได้ |
| **Domain Name** | ชื่อเว็บไซต์ | พื้นฐานการทดสอบเว็บ |
| **Hosting** | ที่เก็บเว็บไซต์ | รู้ว่าเว็บอยู่ที่ไหน |
| **Browser** | โปรแกรมดูเว็บ | เข้าใจว่าบราวเซอร์ render หน้าเว็บยังไง |

**สำหรับ QA:** ต้องรู้เพราะต้องทดสอบว่าเว็บทำงานถูกต้องบนบราวเซอร์ต่างๆ

---

#### 2. What is Testing? (การทดสอบคืออะไร)
**คืออะไร:** ความเข้าใจพื้นฐานเกี่ยวกับการทดสอบซอฟต์แวร์

| หัวข้อย่อย | คืออะไร | สำคัญยังไง |
|-----------|---------|-------------|
| **Testing Definition** | การหาข้อบกพร่อง | เข้าใจว่าทำไมต้องทดสอบ |
| **QA vs QC vs Testing** | ความแตกต่าง 3 อย่าง | QA = กระบวนการ, QC = ตรวจสอบ, Testing = กิจกรรม |
| **Verification vs Validation** | ตรวจสอบ vs ยืนยัน | Verification = ทำถูกต้องมั้ย, Validation = ถูกต้องมั้ย |
| **SDLC** | วงจรพัฒนาซอฟต์แวร์ | รู้ว่าทดสอบใน phase ไหน |
| **STLC** | วงจรทดสอบซอฟต์แวร์ | ขั้นตอนการทดสอบ |

---

#### 3. Testing Types (ประเภทการทดสอบ)
**คืออะไร:** มีการทดสอบกี่แบบ แต่ละแบบทดสอบอะไร

```
Testing Types
│
├── Functional Testing (ทดสอบฟังก์ชั่น)
│   ├── Unit Testing (ทดสอบส่วนเล็กสุด)
│   ├── Integration Testing (ทดสอบการเชื่อมต่อ)
│   ├── System Testing (ทดสอบระบบทั้งหมด)
│   └── Acceptance Testing (ทดสอบก่อนรับมอบ)
│
├── Non-Functional Testing (ทดสอบนอกฟังก์ชั่น)
│   ├── Performance Testing (ความเร็ว)
│   ├── Security Testing (ความปลอดภัย)
│   ├── Usability Testing (ใช้งานง่าย)
│   └── Compatibility Testing (ใช้ได้บนทุกอุปกรณ์)
│
└── Maintenance Testing (ทดสอบหลัง deploy)
    ├── Regression Testing (ทดสอบซ้ำ)
    └── Smoke Testing (ทดสอบคร่าวๆ)
```

---

### **ส่วนที่ 2: การทดสอบด้วยมือ (Manual Testing)** 🟢

#### 4. Test Scenarios & Cases
**คืออะไร:** วิธีเขียนแผนการทดสอบ

| เอกสาร | คืออะไร | ตัวอย่าง |
|--------|---------|----------|
| **Test Scenario** | สถานการณ์ที่ต้องทดสอบ | "ทดสอบการ login" |
| **Test Case** | ขั้นตอนละเอียด | "1. เปิดหน้า login 2. ใส่ username 3. กดปุ่ม..." |
| **Test Data** | ข้อมูลสำหรับทดสอบ | username: test, password: 123 |
| **Test Script** | สคริปต์ automation | โค้ดสำหรับรันอัตโนมัติ |

**Template เขียน Test Case:**
```
Test Case ID: TC001
Title: Login with valid credentials
Precondition: User ต้องมี account แล้ว
Steps:
  1. เปิดหน้า https://example.com/login
  2. ใส่ username: validuser
  3. ใส่ password: validpass
  4. กดปุ่ม Login
Expected Result: ระบบพาไปหน้า Dashboard
Actual Result: (เติมหลังทดสอบ)
Status: Pass/Fail
Priority: High
```

---

#### 5. Testing Techniques (เทคนิคการทดสอบ)
**คืออะไร:** วิธีการทดสอบแบบต่างๆ

| เทคนิค | คืออะไร | ใช้เมื่อไหร่ |
|--------|---------|--------------|
| **Black Box** | ทดสอบโดยไม่ดูโค้ด | ไม่รู้ว่าข้างในเขียนยังไง |
| **White Box** | ทดสอบโดยดูโค้ด | รู้โครงสร้าง code |
| **Grey Box** | ทดสอบบางส่วน | รู้บ้างไม่รู้บ้าง |
| **Exploratory** | ทดสอบแบบสำรวจ | ไม่มี test case |
| **Ad-hoc** | ทดสอบมั่วๆ | หา bug แปลกๆ |

---

#### 6. Bug Reporting (การรายงาน Bug)
**คืออะไร:** เจอ bug แล้วต้องรายงานยังไง

**Template รายงาน Bug:**
```
Bug ID: BUG001
Title: กดปุ่ม Login แล้วไม่เกิดอะไรขึ้น
Severity: Critical (ขั้นวิกฤต) / High / Medium / Low
Priority: High (ต้องแก้ด่วน)

Description:
เมื่อกรอก username/password ถูกต้อง แล้วกดปุ่ม Login
ไม่มีอะไรเกิดขึ้น ไม่ redirect ไปไหน

Steps to Reproduce:
1. เปิด https://example.com/login
2. ใส่ username: test
3. ใส่ password: test123
4. กดปุ่ม Login

Expected Result: ควร redirect ไปหน้า Dashboard
Actual Result: หน้าไม่เปลี่ยน ไม่มี error

Environment:
- Browser: Chrome 120
- OS: Windows 11
- URL: https://example.com/login

Screenshots: [แนบรูป]
```

**Bug Life Cycle (วงจรชีวิต Bug):**
```
New → Assigned → Open → Fixed → Retest → Verified → Closed
          ↑                                    ↓
        Rejected                          Reopen
```

---

### **ส่วนที่ 3: เครื่องมือ (Tools)** 🟡

#### 7. Test Management Tools (เครื่องมือจัดการการทดสอบ)
**คืออะไร:** โปรแกรมช่วยจัดการ test case และ bug

| เครื่องมือ | ใช้ทำอะไร | ราคา |
|-----------|-----------|------|
| **Jira** | จัดการ bug, task, project | มีเวอร์ชั่นฟรี |
| **TestRail** | จัดการ test case | เสียเงิน |
| **TestLink** | จัดการ test case | ฟรี |
| **qTest** | Test management | เสียเงิน |
| **Zephyr** | Jira plugin สำหรับ test | เสียเงิน |

**ทำไมต้องใช้:**
- เก็บ test case เป็นระบบ
- Track สถานะ bug
- สร้างรายงาน (report)
- ทำงานเป็นทีมได้

---

#### 8. API Testing Tools (เครื่องมือทดสอบ API)
**คืออะไร:** โปรแกรมทดสอบ API/Backend

| เครื่องมือ | ใช้ทำอะไร | ข้อดี |
|-----------|-----------|--------|
| **Postman** | ทดสอบ API | ง่าย, มี GUI |
| **SoapUI** | ทดสอบ SOAP API | รองรับ SOAP |
| **Insomnia** | ทดสอบ API | เบา, เร็ว |
| **REST Assured** | API automation (Java) | เขียน code ได้ |
| **cURL** | ส่ง HTTP request | คำสั่ง command line |

**Postman ใช้ยังไง:**
1. เปิด Postman
2. สร้าง Collection
3. สร้าง Request (GET/POST/PUT/DELETE)
4. ใส่ URL
5. ใส่ Headers/Body (ถ้ามี)
6. กด Send
7. ดู Response

---

### **ส่วนที่ 4: Automation Testing** 🟡

#### 9. Test Automation (การทดสอบอัตโนมัติ)
**คืออะไร:** เขียนโปรแกรมให้ทดสอบแทนที่จะกดเอง

**ทำไมต้อง automation:**
- ✅ เร็วกว่า
- ✅ ไม่ผิดพลาด
- ✅ รันได้ซ้ำๆ
- ✅ ประหยัดเวลาในระยะยาว

**เครื่องมือยอดนิยม:**

| เครื่องมือ | ใช้ทดสอบอะไร | ภาษา |
|-----------|--------------|------|
| **Selenium** | Web (บราวเซอร์) | Java, Python, C#, JavaScript |
| **Cypress** | Web สมัยใหม่ | JavaScript |
| **Playwright** | Web (หลายบราวเซอร์) | JavaScript, Python |
| **Appium** | Mobile (iOS/Android) | หลายภาษา |
| **Robot Framework** | ทั่วไป | Python |

---

#### 10. Selenium WebDriver
**คืออะไร:** เครื่องมือยอดนิยมสำหรับ automation testing

**ทำงานยังไง:**
```python
# ตัวอย่าง Selenium (Python)
from selenium import webdriver

# เปิด Chrome
driver = webdriver.Chrome()

# เปิดเว็บ
driver.get("https://example.com")

# หา element (ช่องกรอก username)
username_box = driver.find_element("id", "username")
username_box.send_keys("myusername")

# กดปุ่ม
login_button = driver.find_element("id", "login-btn")
login_button.click()

# ปิด browser
driver.quit()
```

**Locators (วิธีหา element):**
- `id` - หาจาก id (ดีที่สุด)
- `name` - หาจาก name
- `class name` - หาจาก class
- `tag name` - หาจาก tag (div, input)
- `link text` - หาจากข้อความลิงก์
- `partial link text` - หาจากข้อความบางส่วน
- `xpath` - ใช้ XPath expression
- `css selector` - ใช้ CSS selector

---

### **ส่วนที่ 5: Non-Functional Testing** 🔵

#### 11. Performance Testing (ทดสอบประสิทธิภาพ)
**คืออะไร:** ทดสอบว่าระบบทำงานเร็วแค่ไหน รับ load ได้เท่าไหร่

**ประเภท:**

| ประเภท | ทดสอบอะไร | เครื่องมือ |
|--------|-----------|-----------|
| **Load Testing** | รับ user จำนวนมากได้ไหม | JMeter, LoadRunner |
| **Stress Testing** | จุดที่ระบบพังคืออะไร | JMeter |
| **Spike Testing** | รับ traffic กระทันหันได้ไหม | JMeter |
| **Endurance Testing** | ทำงานนานๆ เป็นยังไง | JMeter |

**Metrics (ตัววัด):**
- **Response Time** - ตอบสนองเร็วไหม (ควร < 2 วินาที)
- **Throughput** - จัดการ request ได้กี่ต่อวินาที
- **Error Rate** - อัตรา error (ควร < 1%)
- **CPU/Memory Usage** - ใช้ทรัพยากรเท่าไหร่

**JMeter ใช้ยังไง:**
1. เปิด JMeter
2. สร้าง Test Plan
3. เพิ่ม Thread Group (จำนวน user)
4. เพิ่ม HTTP Request
5. เพิ่ม Listener (ดูผล)
6. กด Run

---

#### 12. Security Testing (ทดสอบความปลอดภัย)
**คืออะไร:** ทดสอบว่าระบบปลอดภัยจากการโจมตีไหม

**OWASP Top 10 (ช่องโหว่ยอดนิยม):**

| ลำดับ | ชื่อ | คืออะไร |
|-------|------|---------|
| A01 | Broken Access Control | เข้าถึงสิ่งที่ไม่ควรเข้าถึงได้ |
| A02 | Cryptographic Failures | การเข้ารหัสผิดพลาด |
| A03 | Injection | SQL Injection, XSS |
| A04 | Insecure Design | ออกแบบไม่ปลอดภัย |
| A05 | Security Misconfiguration | ตั้งค่าผิด |
| A06 | Vulnerable Components | ใช้ library ที่มีช่องโหว่ |
| A07 | Auth Failures | การยืนยันตัวตนพัง |
| A08 | Data Integrity Failures | ข้อมูลไม่สมบูรณ์ |
| A09 | Logging Failures | ไม่มี log ความปลอดภัย |
| A10 | SSRF | โจมตี server ผ่าน request |

**เครื่องมือ:**
- **Burp Suite** - ดักจับและแก้ไข HTTP request
- **OWASP ZAP** - หาช่องโหว่ฟรี
- **Nmap** - สแกน port
- **SQLMap** - หา SQL Injection

---

### **ส่วนที่ 6: CI/CD & DevOps** 🔵

#### 13. CI/CD (Continuous Integration/Deployment)
**คืออะไร:** ระบบ automation สำหรับ build และ deploy

```
Developer push code → Build → Test → Deploy (อัตโนมัติ)
```

**Tools:**
- **Jenkins** - เครื่องมือ CI/CD ยอดนิยม
- **GitHub Actions** - CI/CD ของ GitHub
- **GitLab CI** - CI/CD ของ GitLab
- **CircleCI** - CI/CD cloud
- **Travis CI** - CI/CD cloud

**Jenkins Pipeline:**
```groovy
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
                sh 'npm run build'
            }
        }
        stage('Test') {
            steps {
                sh 'npm test'
            }
        }
        stage('Deploy') {
            steps {
                sh 'deploy'
            }
        }
    }
}
```

---

### **ส่วนที่ 7: Advanced Topics** ⚪

#### 14. Mobile Testing (ทดสอบมือถือ)
**คืออะไร:** ทดสอบแอพบน iOS/Android

**เครื่องมือ:**
- **Appium** - Automation สำหรับ mobile
- **Espresso** - สำหรับ Android
- **XCUITest** - สำหรับ iOS

**สิ่งที่ต้องทดสอบ:**
- ทำงานบน screen size ต่างๆ
- Touch gestures
- Rotate screen
- Battery usage
- Offline mode

---

#### 15. Database Testing (ทดสอบฐานข้อมูล)
**คืออะไร:** ทดสอบว่าข้อมูลถูกต้องในฐานข้อมูล

**ทดสอบอะไร:**
- Data integrity (ข้อมูลสมบูรณ์)
- CRUD operations (Create, Read, Update, Delete)
- Constraints (primary key, foreign key)
- Stored procedures

---

## 🎯 สรุป Learning Path

```
เริ่มต้น ──────────────────────────────────────▶ ระดับสูง

[พื้นฐาน]              [มือใหม่]              [กลาง]              [สูง]
Internet Basics    →  Manual Testing    →  Automation   →  Advanced
Testing Concepts       Test Case Writing      Selenium         Security
SDLC/STLC              Bug Reporting          API Testing      Performance
                      Tools (Jira/Postman)    CI/CD            Mobile
```

---

## 📚 Resources ที่แนะนำ

- [roadmap.sh/qa](https://roadmap.sh/qa) - แผนการเรียนรู้หลัก
- [Selenium Documentation](https://www.selenium.dev/documentation/)
- [Postman Learning](https://learning.postman.com/)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [ISTQB Certification](https://www.istqb.org/)

---

เข้าใจมากขึ้นไหมคะ? มีหัวข้อไหนอยากให้อธิบายเพิ่มไหม? 🐰
